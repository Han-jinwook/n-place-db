# N-Place-DB 기능 명세서

## 1. 개요
N-Place-DB 애플리케이션의 주요 기능과 구조를 정의합니다.

## 2. 주요 기능
### 2.1 크로스셀링(Cross-selling) 프로모션 배너 시스템
* **위치**: 앱 하단 푸터 (고객센터 영역의 좌측)
* **방식**: 
  - DB(Supabase `promotions` 테이블)에서 `is_active=True`인 프로모션 데이터를 동적으로 불러옴.
  - 현재 앱(`NPlace-DB`)을 홍보하는 자체 배너(`promote_product_id='NPlace-DB'`)는 필터링하여 제외.
  - 남은 배너들이 여러 개일 경우, **매 시간(hour) 단위로 롤링(교차) 노출** 처리 (`current_hour % len(filtered_promos)`).
  - DB에 `image_url`이 등록되어 있으면 가로형 이미지 띠 배너로 노출 (높이 50px로 자동 맞춤).
  - 이미지가 없으면 기존 텍스트 형태의 세련된 뱃지 UI로 대체(Fallback).
* **목적**: 앱 업데이트 없이도 DB 수정만으로 즉각적인 타 제품 홍보 및 이벤트 공지 가능.

### 2.2 가이드 페이지 (Showroom 연동)
* 데스크탑 앱 내부에 무거운 설명서를 두지 않고, `3Monster` 쇼룸 웹사이트의 제품 가이드 페이지로 바로가기 링크 제공.
* 가이드 페이지에서 취소/뒤로가기 시 기존의 앱 상세 설명 팝업/모달 상태가 그대로 유지되도록 상태(state) 처리.
* 제품명 별도 표기 및 제품 특성에 맞춘 핀포인트 설명 제공.

### 2.3 배포 및 OTA 로직 (보안 및 난독화 필수)
* **소스 코드 보안 (PyArmor 난독화 필수)**: 
  - 핵심 비즈니스 로직 및 크롤러, 인증 스크립트(`step1_refined_crawler.py`, `auth.py`, `sb_auth_manager.py`)는 **PyArmor 정식 라이선스(`Sundream` / `pyarmor-vax-012488`)**를 사용하여 정식 난독화 처리 후 패키징.
  - 다른 프로그램을 개발/제품화할 때도 반드시 정식 난독화 라이선스를 통한 소스 코드 보안 적용 필수 (코드 유출 방지 철칙).
* **빌드 파이프라인**: `build_exe.py` 빌드 스크립트를 통해 PRO 버전과 TRIAL(무료체험판) 버전을 각각 자동 분리하여 패키징.
* **배포 및 자동 업데이트(OTA)**: Github Releases를 통해 버전 번호가 기재된 최신 zip 파일(예: `Map_DB-Pro-v1.1.72.zip`, `Map_DB-Trial-v1.1.72.zip`)을 배포하고 Supabase `app_versions` DB 동기화. 내부의 실행 폴더/파일 이름은 업데이트 안정성을 위해 버전 번호 없이 고정.
* **무료체험판(TRIAL) 관리**: 
  - `trial_logs` 테이블에 UPSERT 방식으로 사용자별 누적 수집 건수 및 정보를 기록 (초기화 및 중복 생성 방지).
  - 어드민 대시보드(Hub)의 **프로그램별 체험판 현황** 메뉴에서 개별 HWD 기기별 사용 기록을 모니터링하고 삭제(초기화)하여 재체험 부여 가능.
