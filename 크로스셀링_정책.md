# 3Monster 생태계 크로스셀링(Cross-selling) 정책

## 1. 개요
3Monster의 제품군(앱) 내부 공간을 활용하여 타 제품을 홍보하거나 중요한 공지사항을 전달하는 마케팅 시스템 아키텍처.
단순히 글자가 움직이는 촌스러운 마키(Marquee) 텍스트 형태를 지양하고, 세련된 고정형 UI 카드와 데이터베이스 기반의 롤링(교체) 시스템을 구축한다.

## 2. 아키텍처 원칙 (Supabase 연동)
* **하드코딩 금지**: 프로그램 소스 코드 내부에 광고 문구를 직접 작성하지 않는다.
* **DB 기반 송출**: Supabase에 `promotions` (또는 `cross_sell_banners`) 테이블을 생성하여 광고 데이터를 관리한다.
* **동적 롤링**: 앱이 실행될 때마다, 혹은 일정 주기(예: 1시간)마다 DB에서 활성화된 광고 목록을 가져와 그 중 하나를 랜덤 또는 순차적으로 노출한다.
* **무중단 업데이트**: 새로운 제품이 출시되거나 할인 이벤트가 있을 경우, 사용자가 앱을 업데이트할 필요 없이 관리자가 Supabase 테이블의 값만 추가/수정하면 모든 사용자의 앱에 즉시 반영된다.

## 3. UI/UX 가이드라인 및 공통 디자인 규격
* **위치**: 데스크탑 앱의 좌측 사이드바 하단 혹은 메인 화면의 최하단 푸터 영역.
* **형태**: 눈을 피로하게 하는 애니메이션을 배제하고, 깔끔한 **고정형 텍스트 카드** 또는 **배지(Badge) 스타일**을 사용한다.
* **액션**: 클릭 시 사용자의 기본 웹 브라우저를 통해 3Monster 쇼룸의 해당 제품 상세 페이지로 랜딩(`st.link_button` 또는 HTML 앵커 태그 활용).
* **배너 디자인 규격 (3Monster 생태계 공통 필수 준수 사항)**:
  - **좌측 크로스셀링 홍보 배너**: `800px × 120px` (가로형 띠 모양, 약 6:1 비율)
  - **우측 고객센터 고정 배너**: `400px × 120px` (짧은 가로형 뱃지 모양, 약 3:1 비율)
  - 두 배너 모두 프로그램 하단의 동일한 높이(120px 기준)로 통일감을 맞추기 위해 모든 앱에서 위 규격을 엄격히 준수하여 디자인되어야 함.

## 4. 데이터베이스 구조 (예시)
Supabase `promotions` 테이블 설계 방향:
* `id` (uuid)
* `product_target` (text): 광고가 노출될 앱 (예: 'all', 'NPlace-DB' 등)
* `title` (text): 노출될 텍스트 (예: "✨ [신제품] 인스타 마케팅 몬스터 출시!")
* `url` (text): 클릭 시 이동할 랜딩 페이지 URL
* `is_active` (boolean): 노출 여부 (ON/OFF 제어용)
* `priority` (integer): 노출 우선순위 또는 확률 설정용

## 5. 도입 시기
* **현재 상태**: 정책 확립 및 문서화 완료.
* **실제 코드 구현**: 다음 번 정기 기능 업데이트(마이너 패치) 배포 시 앱 내부에 빈 자리(DB 페칭 로직 포함)를 구축하여 포함시킨다. (빌드 리소스 절약)
* **본격 가동**: 3Monster의 두 번째 프로덕트가 정식 출시되는 시점에 Supabase에 첫 광고 데이터를 삽입하여 가동을 시작한다.
